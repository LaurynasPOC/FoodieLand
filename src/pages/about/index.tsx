import React from "react";

import { GridWrapper } from "components/wrappers/GridWrapper";

const About: React.FC = () => {
  return <GridWrapper bg="primary">About</GridWrapper>;
};

export default About;
